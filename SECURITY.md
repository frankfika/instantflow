# 安全说明

InstantFlow 用浏览器端 ECDH P-256 协商共享秘密，以 HKDF-SHA-256 绑定双方公钥并派生 AES-256-GCM 内容密钥和安全校验码。服务端只保存密文、房间状态和经过哈希的授权令牌；它不接收双方私钥或协商出的内容密钥。

## 安全边界

- HTTPS 保护网页与接口传输，端到端加密保护临时内容。
- 8 位配对码只负责找到房间，不是加密密钥；连续猜码会按来源跨节点限流。
- 发送端必须确认双方显示相同的安全校验码后才会加密上传。校验码不一致表示可能配错设备或遭到主动中间人攻击，应立即销毁房间。
- 密文默认保存 1–10 分钟，接收成功或发送方撤销后立即删除。
- 每个房间由独立 Durable Object 管理，密文分块保存在该房间自己的持久化空间。
- 应用结构化日志不记录配对码、房间 ID、访问令牌、IP 或用户内容。

和所有浏览器端端到端加密产品一样，如果网页本身被恶意替换，恶意脚本仍可能在加密前读取内容。因此“服务端看不到内容”指正常运行且前端代码未被篡改的安全模型，不应宣传为能够抵御服务提供方主动投毒的绝对零知识。

“仅限同一网络出口”使用服务端观察到的网络地址指纹进行强制校验：IPv4 要求地址一致，IPv6 要求 `/64` 前缀一致。它不读取 SSID，也不能严格证明两台设备连接的是同一个物理路由器；企业 NAT、VPN、代理、蜂窝网络和隐私中继可能造成误判。因此它是一项可选的收紧措施，不能替代端到端加密和附加接收口令。

附加接收口令在发送端使用每个房间独立的随机盐和 PBKDF2-SHA-256（210,000 次）生成用途隔离的校验值与加密材料；服务端再哈希后保存校验值，不保存可直接提交的验证令牌。口令生成的加密材料会通过 HKDF 参与端到端内容密钥派生。口令强度仍由用户决定，建议至少 8 个字符，并通过与配对码不同的渠道告知接收方。

安全校验码能发现配对抢占和主动公钥替换，但只有双方实际比较时才提供这项保证。浏览器下载会强制使用 `application/octet-stream`，文件名会移除路径分隔符和控制字符；服务端从不把上传密文作为可执行网页提供。

## 报告漏洞

请使用 GitHub 仓库 Security 页面中的 **Report a vulnerability** 私密报告功能。报告请包含影响、复现步骤、受影响版本和建议修复方式，不要在公开 Issue 中披露可利用细节。

当前 `0.x` 版本只为最新提交提供安全修复。确认漏洞后，维护者会先私下复现和制定修复，再协调披露时间；涉及真实数据风险时会优先发布修复和轮换部署凭据。
