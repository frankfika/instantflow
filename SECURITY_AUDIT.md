# InstantFlow 开源安全审计报告

审计日期：2026-08-02  
范围：浏览器端、Node.js 参考服务、Cloudflare Worker / Durable Objects、依赖与锁文件、CI/CD、仓库文件卫生和安全文档。

## 结论

本轮识别出的高、中风险实现问题均已修复，自动化检查、Cloudflare 本地边缘冒烟和双浏览器端到端流程均通过。当前代码具备进入公开代码审查的安全基线，但这不等同于“绝对安全”或第三方密码学审计认证；发布前仍需完成本文末尾的仓库平台设置。

## 已修复问题

| 级别 | 问题 | 处理结果 |
|---|---|---|
| 高 | ECDH 公钥未认证，首个配对设备会立即收到内容 | 双方从绑定两把公钥的 HKDF 会话派生安全校验码；发送端必须人工确认一致后才加密上传 |
| 高 | 接收令牌以明文保存在房间状态，和文档承诺不符 | 令牌只在首次配对响应返回一次；Node 与 Durable Object 状态均只保存 SHA-256 哈希；重复配对不会再次返回令牌 |
| 中 | 6 位数字配对码抗在线猜测空间有限 | 提升为 8 位数字；跨节点查询限制收紧为每来源 5 分钟 10 次 |
| 中 | 接收口令的验证值可从持久化状态直接重放 | 浏览器以独立随机盐执行 PBKDF2-SHA-256 210,000 次，分离验证值与加密材料；服务端再哈希保存验证值；加密材料参与 HKDF |
| 中 | GitHub Actions 使用浮动大版本标签 | 所有 Actions 固定到不可变提交；最小权限；禁用 checkout 凭据持久化；加入 Gitleaks、CodeQL 和 Dependabot |
| 中 | `package-lock.json` 与依赖清单不同步，`npm ci` 会失败 | 重新生成锁文件并通过 `npm ci --ignore-scripts` 验证 |
| 低 | EC JWK 只验证字符串格式，未验证曲线点 | Node 和 Worker 均实际导入 P-256 公钥，拒绝无效曲线点，并只保存规范字段 |
| 低 | 解密后的文件名和 MIME 类型直接用于浏览器下载 | 限制信封结构和长度、清理路径/控制字符、校验声明大小，下载强制为 `application/octet-stream` |
| 低 | 生产响应与发布配置缺少部分显式约束 | 增加 CORP、收紧 CSP、生产同源限制、静态资源只允许 GET/HEAD、关闭生产 source map |
| 低 | 存在未使用的旧版链接传输流程 | 删除旧实现和未使用依赖引用，减少误审和攻击面 |

## 验证结果

| 检查 | 结果 |
|---|---|
| Node API 与密码学测试 | 13/13 通过 |
| Worker TypeScript 类型检查 | 通过 |
| Vite 生产构建（无 source map） | 通过 |
| Wrangler 生产部署 dry-run | 通过 |
| npm 官方安全公告库 | 0 个已知漏洞（严重、高、中、低均为 0） |
| 可复现无脚本安装 | `npm ci --ignore-scripts` 通过 |
| Cloudflare 本地边缘冒烟 | 通过：分块密文、令牌鉴权、逐字节下载、一次性删除、同源与安全头 |
| 双浏览器真实流程 | 通过：8 位配对、双方校验码一致、人工确认后上传、本地解密、服务端删除确认 |
| 浏览器控制台 | 发送端与接收端均无 warning/error |
| 当前工作树秘密模式扫描 | 未发现常见格式的真实密钥、令牌或私钥 |

## 仍然存在的安全边界

1. 如果生产网页或发布账号被攻陷，恶意前端可以在加密前读取内容。CSP、固定依赖和 CI 能降低风险，但无法消除浏览器端 E2EE 的这一固有边界。
2. 安全校验码只有在双方实际比较时才能发现配对抢占或主动公钥替换；不比较就没有身份确认保证。
3. 8 位配对码仍不是密码学密钥。高价值传输应同时启用独立接收口令，并通过另一条可信渠道告知接收方。
4. “同一网络出口”只是 IPv4 地址或 IPv6 `/64` 前缀启发式限制，会受 NAT、VPN、代理、蜂窝网络和隐私中继影响。
5. Cloudflare 仍可能按平台政策处理原始网络元数据；应用日志已参数化敏感路径，但平台访问日志不完全由应用控制。
6. `server.mjs` 是单进程、内存型参考服务，不提供生产级持久化或分布式限流；生产部署应使用 Worker + Durable Objects。
7. 本目录当前没有 `.git`，因此本轮无法扫描历史提交中曾经出现后又删除的秘密。初始化或迁入正式仓库后必须让 Gitleaks 对完整历史执行一次扫描。
8. 本轮不是独立第三方密码学认证，也未完成大规模 DDoS/容量压测。高风险正式运营前建议再做外部渗透测试。

## 公开仓库发布前人工清单

- 在 GitHub 启用 Secret scanning、Push protection、CodeQL、Dependabot 和 Private vulnerability reporting。
- 为 `main` 设置分支保护，要求 CI、Gitleaks 和 CodeQL 通过，禁止强推和绕过审核。
- 选择并添加开源许可证；当前仓库尚无 `LICENSE`，许可证类型需要项目所有者决定。
- 确认 `SECURITY.md`、`PRIVACY.md` 中的运营主体、适用地区和数据请求渠道符合实际上线安排。
- 使用自定义域名后关闭不需要的 `workers.dev` / preview URL；发布凭据使用最小权限并定期轮换。
- 首次公开推送后检查 GitHub Security 页面无未处理告警，再发布正式版本标签。
